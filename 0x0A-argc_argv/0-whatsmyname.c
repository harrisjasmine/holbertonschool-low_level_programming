#include <stdio.h>

/**
 * main - main function to print prog name
 * @argc: number of arguments
 * @argv: array of pointers to string
 */

int main (int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
